FROM golang:1.24-alpine AS builder

WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download

COPY . .
RUN go build -o /benchmark ./benchmark/benchmark.go

FROM alpine:latest
RUN apk --no-cache add ca-certificates

COPY --from=builder /benchmark /benchmark

ENTRYPOINT ["/benchmark"]